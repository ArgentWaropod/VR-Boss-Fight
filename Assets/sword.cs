using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class sword : MonoBehaviour
{
    private void OnTriggerEnter(Collider other)
    {
        /*if (other.GetComponent<hydra>())
        {
            other.GetComponent<hydra>().SliceHead();
        }*/
    }
}
